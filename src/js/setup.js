'use strict';
;(function (root, undefined) {

	// Setup a simple global App variable where we can define our components on
	// Run before everything inside directory is loaded
	root.App = {};

})(this);
